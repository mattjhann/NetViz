// Single source of truth for the encapsulation walkthrough.
//
// The encapsulation flow is modelled top-down (sender side) following the
// TCP/IP stack. Each layer takes the Protocol Data Unit (PDU) handed down from
// the layer above, wraps it with its own header (and, for the data-link layer,
// a trailer), and passes the new, larger PDU to the layer below.
//
// `headerFields` / `trailerFields` entries are `{ name, exampleValue, detail }`
// where `detail` is a longer, hover-revealed explanation of that field's job.

export const LAYERS = [
  {
    id: 'application',
    layerName: 'Application',
    layerNumber: 'Layer 7–5',
    pduName: 'Data',
    accentColor: '#7c8cff',
    protocol: 'HTTP',
    description:
      'Your app produces a message. Here it is an HTTP request — the actual content you want to send. At this stage it is just "data" with no networking information attached yet.',
    scrollHint: 'This is the payload everything else will wrap around.',
    // The application layer contributes the payload itself rather than a header.
    payload: {
      label: 'Application Data',
      exampleValue: 'GET /index.html HTTP/1.1',
      detail:
        'The raw message generated by the application (an HTTP request in this example). Every layer below treats this — plus everything already added — as opaque payload to be carried.',
    },
    headerFields: [],
    trailerFields: [],
  },
  {
    id: 'transport',
    layerName: 'Transport',
    layerNumber: 'Layer 4',
    pduName: 'Segment',
    accentColor: '#42d6a4',
    protocol: 'TCP',
    description:
      'TCP prepends a header to the data, turning it into a Segment. This header makes the data reliable and lets the receiver hand it to the right application.',
    scrollHint: 'Ports identify the apps; sequence numbers keep bytes in order.',
    headerFields: [
      {
        name: 'Src Port',
        exampleValue: '49152',
        detail:
          'The sending application\'s port number. Together with the source IP it identifies exactly which process on this machine opened the connection.',
      },
      {
        name: 'Dst Port',
        exampleValue: '443',
        detail:
          'The destination application\'s port. 443 means HTTPS, so the receiver knows to hand this segment to its web/TLS server process.',
      },
      {
        name: 'Seq #',
        exampleValue: '1029384756',
        detail:
          'Sequence number — the position of this segment\'s first byte in the overall stream. It lets the receiver reassemble bytes in the correct order even if they arrive out of order.',
      },
      {
        name: 'Ack #',
        exampleValue: '0',
        detail:
          'Acknowledgement number — the next sequence number the sender expects to receive. This is how TCP confirms delivery and drives retransmission of lost data.',
      },
      {
        name: 'Flags',
        exampleValue: 'SYN',
        detail:
          'Control bits (SYN, ACK, FIN, RST, PSH, URG) that manage the connection lifecycle — opening, acknowledging, and tearing down the session.',
      },
      {
        name: 'Window',
        exampleValue: '64240',
        detail:
          'Receive window size — how many bytes the receiver is currently willing to accept. This is the basis of TCP flow control, preventing a fast sender from overwhelming a slow receiver.',
      },
      {
        name: 'Checksum',
        exampleValue: '0x1f4a',
        detail:
          'A checksum over the header and data so the receiver can detect corruption in the segment and discard it if the bits were damaged in transit.',
      },
    ],
    trailerFields: [],
  },
  {
    id: 'network',
    layerName: 'Network',
    layerNumber: 'Layer 3',
    pduName: 'Packet',
    accentColor: '#ffb454',
    protocol: 'IP',
    description:
      'IP wraps the segment in its own header, producing a Packet (datagram). This header carries the logical addresses needed to route the data across networks to the right host.',
    scrollHint: 'Source and destination IP addresses enable end-to-end routing.',
    headerFields: [
      {
        name: 'Version',
        exampleValue: '4',
        detail:
          'IP version number. 4 means IPv4. Routers read this first to know how to interpret the rest of the header.',
      },
      {
        name: 'TTL',
        exampleValue: '64',
        detail:
          'Time To Live — a hop counter decremented by each router. When it reaches zero the packet is discarded, preventing packets from looping forever around the network.',
      },
      {
        name: 'Protocol',
        exampleValue: '6 (TCP)',
        detail:
          'Identifies the protocol of the payload inside this packet. 6 means the payload is a TCP segment, so the receiving host knows to pass it up to TCP.',
      },
      {
        name: 'Src IP',
        exampleValue: '192.168.1.24',
        detail:
          'The logical address of the sending host. Replies are routed back to this address across the internet.',
      },
      {
        name: 'Dst IP',
        exampleValue: '93.184.216.34',
        detail:
          'The logical address of the destination host. Every router along the path inspects this field to decide where to forward the packet next.',
      },
      {
        name: 'Checksum',
        exampleValue: '0xb1e6',
        detail:
          'A checksum covering only the IP header. Routers recompute it at each hop (the TTL changes), letting them detect header corruption.',
      },
    ],
    trailerFields: [],
  },
  {
    id: 'datalink',
    layerName: 'Data Link',
    layerNumber: 'Layer 2',
    pduName: 'Frame',
    accentColor: '#ff6b8b',
    protocol: 'Ethernet',
    description:
      'Ethernet adds both a header and a trailer, forming a Frame. This is the only layer to add a trailer. The header carries physical (MAC) addresses for the next hop; the trailer carries an error-check.',
    scrollHint: 'MAC addresses move the frame across one physical link at a time.',
    headerFields: [
      {
        name: 'Dst MAC',
        exampleValue: '3c:22:fb:8a:1d:e0',
        detail:
          'The hardware (MAC) address of the next device on the local link — often your router/gateway. Unlike IP addresses, MAC addressing only needs to reach the next hop, not the final host.',
      },
      {
        name: 'Src MAC',
        exampleValue: 'a4:83:e7:2c:90:11',
        detail:
          'The hardware address of this network interface, so the next device knows where the frame came from on the local network.',
      },
      {
        name: 'EtherType',
        exampleValue: '0x0800',
        detail:
          'Identifies the protocol of the frame\'s payload. 0x0800 means IPv4, telling the receiver to hand the contents up to the IP layer.',
      },
    ],
    trailerFields: [
      {
        name: 'FCS',
        exampleValue: '0x9d4e7af2',
        detail:
          'Frame Check Sequence — a CRC computed over the whole frame. The receiver recomputes it and drops the frame if it does not match, catching bits flipped during physical transmission.',
      },
    ],
  },
  {
    id: 'physical',
    layerName: 'Physical',
    layerNumber: 'Layer 1',
    pduName: 'Bits',
    accentColor: '#9d7bff',
    protocol: 'Signal',
    description:
      'Finally the entire frame is serialized into a stream of bits and converted to a physical signal — electrical voltages, light pulses, or radio waves — that travels across the wire or air.',
    scrollHint: 'No new header is added; the frame becomes raw 1s and 0s on the medium.',
    // The physical layer adds no header; it transforms the frame into a signal.
    payload: {
      label: 'Bit / Signal Stream',
      exampleValue: '01001000 01010100 01010100 01010000 …',
      detail:
        'The complete frame encoded as a sequence of bits and transmitted as a physical signal on the medium. The receiving end reverses every step above — decapsulation — to recover the original data.',
    },
    headerFields: [],
    trailerFields: [],
  },
];

export default LAYERS;
